# Задание: Математические функции

## Часть 1: Факториал
Реализуйте функцию вычисления факториала.

```python
def factorial(n: int) -> int:
    """Вычисляет n! (факториал n)"""
    pass
```

## Часть 2: Числа Фибоначчи

Реализуйте функцию вычисления n-го числа Фибоначчи.

```python
def fibonacci(n: int) -> int:
    """Вычисляет n-е число Фибоначчи"""
    pass
```

## Часть 3: Наибольший общий делитель

Реализуйте функцию вычисления НОД двух чисел (алгоритм Евклида).

```python
def gcd(a: int, b: int) -> int:
    """Вычисляет наибольший общий делитель a и b"""
    pass
```

## Примеры

```python
# Факториал
factorial(0) = 1
factorial(5) = 120

# Числа Фибоначчи
fibonacci(0) = 0
fibonacci(7) = 13

# НОД
gcd(48, 18) = 6
gcd(17, 13) = 1
```

## Ограничения

    Для факториала: 0 ≤ n ≤ 20 (из-за переполнения)

    Для Фибоначчи: 0 ≤ n ≤ 50

    Для НОД: -10^9 ≤ a, b ≤ 10^9

## Критерии оценивания

    Корректность всех функций (60%)

    Обработка edge cases (20%)

    Эффективность алгоритмов (10%)

    Качество кода (10%)

## Примечания

    Учтите, что факториал 0 = 1

    Числа Фибоначчи: F(0)=0, F(1)=1, F(n)=F(n-1)+F(n-2)

    Для отрицательных чисел в НОД используйте абсолютные значения