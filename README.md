# Test Case Generator 🧪

Генератор тестовых случаев для задач по программированию. Инструмент помогает преподавателям создавать уникальные тестовые задания для студентов.

## ✨ Особенности

- ✅ Генерация тестов для разных типов задач
- ✅ Поддержка edge cases
- ✅ Множественные форматы экспорта (JSON, YAML, Python)
- ✅ CLI интерфейс
- ✅ CI/CD с автоматической генерацией
- ✅ Полная типизация и тестирование

## 📦 Установка

### Базовая установка
```bash
git clone https://github.com/username/test-case-generator.git
cd test-case-generator
pip install -e .
```

## Установка для разработки

```bash
pip install -e ".[dev]"
pre-commit install
```

## 🚀 Использование

### Командная строка

```bash
# Генерация тестов для сортировки
testgen sorting -n 10 -o tests.json

# Генерация тестов для поиска
testgen searching --format yaml

# Генерация математических задач
testgen math -n 5 --no-edge-cases
```

## Программное использование

```python
from src.generators.sorting_generator import SortingGenerator
from src.utils.exporter import Exporter

generator = SortingGenerator()
test_cases = generator.generate_all(n_normal=5)
Exporter.to_json(test_cases, "my_tests.json")
```

## 📁 Поддерживаемые типы задач
1. Сортировка

    Обычные массивы

    Edge cases (пустые, отсортированные, обратные)

    С дубликатами

    С отрицательными числами

2. Поиск

    Бинарный поиск в отсортированных массивах

    Линейный поиск

    Элемент присутствует/отсутствует

3. Математические задачи

    Факториал

    Числа Фибоначчи

    Наибольший общий делитель (НОД)

    Проверка на простое число

## 📊 Пример вывода

```json
[
  {
    "input": [64, 34, 25, 12, 22, 11, 90],
    "expected": [11, 12, 22, 25, 34, 64, 90],
    "description": "Нормальный случай 1: массив из 7 элементов",
    "is_edge_case": false,
    "weight": 1.0
  },
  {
    "input": [],
    "expected": [],
    "description": "Пустой массив",
    "is_edge_case": true,
    "weight": 1.5
  }
]
```

## 🧪 Тестирование

```bash
# Запуск всех тестов
make test

# С покрытием кода
pytest tests/ --cov=src --cov-report=html

# Только конкретный тест
pytest tests/test_sorting_generator.py -v
```

## 🔧 Разработка
### Структура проекта

```text
src/
├── generators/     # Генераторы тестовых случаев
├── utils/         # Вспомогательные утилиты
└── main.py        # CLI интерфейс
```

## Code Style

Проект использует:

    Black для форматирования

    Flake8 для линтинга

    MyPy для проверки типов

    isort для сортировки импортов

```bash

# Проверить код
make lint

# Автоформатирование
make format
```